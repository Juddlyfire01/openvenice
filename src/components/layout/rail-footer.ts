/** Shared bottom rail footer — keeps sidebar settings & Intel cost meter aligned. */
export const RAIL_FOOTER_CLASS =
  'shrink-0 border-t border-[var(--color-border-faint)] px-2 py-2.5'

/** Fixed inner height — single-row controls (e.g. settings button). */
export const RAIL_FOOTER_ROW_CLASS =
  'flex h-9 items-center px-2.5 min-w-0'

/** Fixed inner height — two stacked rows (e.g. cost meter). */
export const RAIL_FOOTER_STACK_CLASS =
  'flex h-9 flex-col justify-center gap-px px-2.5 min-w-0'
